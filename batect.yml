project_name: batect-playground

containers:
  database:
    image: mysql:8.0

  go:
    image: golang:1.16

tasks:
  start-dependencies:
    dependencies:
      - database
    run:
      container: go
      command: echo starting
